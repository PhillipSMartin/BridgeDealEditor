# Bridge Deal Editor

A bridge deal editor built with Vite, TypeScript, React, shadcn-ui, and Tailwind CSS.

## Running locally

```sh
npm install
npm run dev
```

The dev server starts at http://localhost:8080.

## Building

```sh
npm run build
```
